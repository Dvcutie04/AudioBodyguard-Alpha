# AQSS validation evidence

This matrix records repository restoration and subsequent development evidence. Software conformance and physical qualification are separate requirements.

| Area | Evidence available | Limit or remaining check |
| --- | --- | --- |
| Phone Python checkout | User-provided output from the separate clone at `38d273e1abec4b979950290ed1bac8cf640a8161`: 1,641 passed in 7.50s on Python 3.13/iOS; generated contracts current; `lg2 status -s` empty | Existing dateutil deprecation warning; predates the retry/invalidation change; Python execution does not qualify physical output or execute the C lab |
| Uploaded source and supplement | 423 original and 18 supplemental file hashes verified; 17 usable supplement files imported | One preexisting nonparseable, unimported module retired with history preserved |
| Every exported source path | [Synchronization manifest](a-shell-sync-manifest.json) accounts for 441 paths: 431 exact, nine maintained, one retired | Describes the supplied a-Shell exports, not later edits on the phone |
| Endpoint and fencing regression tests | 47 tests reproduced on Python 3.12/Linux | Software reference implementations and fixtures |
| N1 native-work and context simulation | Eight work-ordering plus eight context-invalidation cases; 35 focused checks and 1,641 full-suite tests passed locally | One in-memory, test-only resource and handoff; no real restart persistence, secure clock, route atomicity, native output qualification, or production readiness |
| N2a C11 owner | Six compiled scripted-backend cases: partial transfer/hold, positive progress, in-flight hold, delayed accounting, trace capacity, and invalid returns; first retry case failed before admission was checked | Single thread and stable fixture objects; no hardware, OS error policy, gain path, persistence, or positive readiness |
| N2a callback metadata | Five C cases cover queued/active retention, positive progress, stale replay after actual heap reclamation, invalid tickets, and bounded cleanup; the queued-reference test failed before the minimal fix | Complete scripted ticket registry with a stable context; no real OS callback quiescence, recycled context, cross-runtime identity, or thread synchronization |
| N2a retained children | Four C cases cover children outliving parents, closed acquisition, invalid/duplicate releases, and cleanup at capacity; the first reclamation test failed before the retained-child guard | One-level, explicitly registered references; last access must precede release; no child executor, untracked pointers, multiple-runtime retirement, or native shutdown proof |
| N2a acknowledgement identity | Six C cases cover old requests/runtimes, complete scope/status, accounting, clock/deadline rejection, invalid inputs, and one-time binding; stale acceptance and deadline refresh each reproduced before their fix | Internal value matching with trusted fixture IDs and injected clock ticks; no authentication, real clock, transport, or production authority |
| N2a metadata retirement | Five C cases cover capacity, actual heap reclamation, slot reuse with stale-handle/ack rejection, pending accounting, reservations, and invalid handles | Two metadata-block slots in one live pool; external contexts/history remain alive; no byte budget, durable incarnation allocator, OS quiescence, or process recovery |
| N2a retry and runtime invalidation | Nine C cases cover exact suffix retry across zero/EAGAIN, immediate fault inhibition, recovery without replay, hold checks, pre-dispatch faults, both accounting windows, trace capacity, and callback/child cleanup; EAGAIN and delayed-inhibition failures reproduced before fixes | Scripted errno-style returns and injected fault reasons; no actual audio API, retry scheduler, automatic recovery, full fault journal, or physical finality |
| N2a memory instrumentation | Separate CI step uses AddressSanitizer and UBSan; local address/UB checks disable leak detection because LeakSanitizer cannot run under workspace tracing | Instrumented deterministic executions do not prove all schedules or physical exclusion; see the exact revision's hosted result |
| Full restored Python suite | 1,625 passed on local Python 3.12/Linux and hosted Python 3.13/Linux; all active source/test files parse | Software tests do not qualify physical output |
| Diagnostic with older GitHub dependencies | 1,615 passed, 10 failed before supplement | Older interface mismatches resolved with exact phone files |
| Publication-failure test clocks | Two failures reproduced with a 46-second default clock; both passed after explicit synthetic clock injection, followed by all 1,625 tests locally | Existing monotonic and fail-closed assertions retained |
| Generated feedback models | Generator check reported `NATIVE_CONTRACTS_CURRENT` locally and in hosted CI | Contract generation is not a production runtime proof |
| Swift contract conformance | Hosted macOS Swift package compilation and tests passed | Synthetic fixtures do not verify physical output |
| Android contract conformance | Hosted Android Gradle library compilation and tests passed | Synthetic fixtures do not verify physical output |
| Endpoint retirement and physical output | Synthetic test-only trace contract exists | No qualified native output boundary or acoustic observation established |
| Production execution | Endpoint handoff barrier and bridge execution guard stay closed | Software test results do not authorize removing these guards |

All three hosted jobs passed after source cleanup in [Actions run 36067699270](https://github.com/Dvcutie04/AudioBodyguard-Alpha/actions/runs/36067699270); use the [workflow runs](https://github.com/Dvcutie04/AudioBodyguard-Alpha/actions/workflows/ci.yml) to verify the latest revision. Historical benchmark claims in older reports have not been revalidated in this restoration. A mock adapter or a test named “hardware-in-the-loop” is not evidence that a physical device was observed. See [restoration status](repository-restoration.md) for source provenance and remaining work.
